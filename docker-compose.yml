version: "3.9"
services:
  bot:
    build: .
    env_file: .env
    depends_on:
      - mongo
    entrypoint: ["/bin/sh", "-c", "sleep 5 && python -m src.main"]
  mongo:
    image: mongo:6
    volumes:
      - mongo_data:/data/db
volumes:
  mongo_data:
